name: One-shot Slint child-window fix

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  patch-and-check:
    if: github.event.head_commit.message == 'chore: trigger Slint child-window fix'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Patch