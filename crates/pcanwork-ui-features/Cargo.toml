[package]
name = "pcanwork-ui-features"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
publish = false

[dependencies]
slint = { version = "1.17.1", default-features = false, features = ["std", "compat-1-2", "raw-window-handle-06"] }

[build-dependencies]
slint-build = "1.17.1"
